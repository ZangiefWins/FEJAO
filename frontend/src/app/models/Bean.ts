export class Bean {
    constructor(id: number, name: string, lot: number) {
        this.id = id;
        this.name = name;
        this.lot = lot;
    }

    id: number;
    name: string;
    lot: number;
}